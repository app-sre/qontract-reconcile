{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "cmd=$(jq -r '.tool_input.command'); if echo \"$cmd\" | grep -qE '^git (commit|push)'; then make linter-test && make types-test && make unittest; fi",
            "timeout": 600,
            "statusMessage": "Running ruff, mypy, tests before git commit/push..."
          }
        ]
      }
    ]
  }
}
