---
description: Document an existing qontract-api integration
scope: project
---

Invoke the `document-api-integration` skill to document the specified integration (or ask the user which one). Follow the skill's workflow to analyze the integration code and generate documentation.
