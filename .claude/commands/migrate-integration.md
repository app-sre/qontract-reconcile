---
description: Migrate a reconcile/ integration to qontract-api
scope: project
---

Invoke the `migrate-integration` skill to migrate the specified integration (or ask the user which one) from reconcile/ to the qontract-api architecture. Follow the skill's phases: Discovery, Shared Utils, Server-Side, Client-Side, Tests, Documentation.
