# This file isn't automatically generated, but it is a re-export of the exceptions from the `clientele` package.
# This allows users of `qontract-api-client` to import exceptions directly from this package
# without needing to know about the underlying `clientele` package.
from clientele.api.exceptions import APIException, HTTPStatusError

__all__ = ["APIException", "HTTPStatusError"]
